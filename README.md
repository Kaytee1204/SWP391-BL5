# Japanese Learning Platform - Backend API (Spring Boot 3 + SQL Server)

Dự án  **Nền tảng Học tiếng Nhật (Japanese Learning Platform)** sử dụng **Spring Boot 3.3.x**, **Java 17+**, **Spring Security 6**, **JWT**, **Spring Data JPA**, **Microsoft SQL Server (Database: SWP391)**, và **Swagger OpenAPI 3 (KhanhTB)**.

---


## 🚀 Các Feature chính đang hoạt động

#################################

---

## 🚀 Khởi chạy và Test API

1. Chạy ứng dụng:
   ```bash
   mvn clean spring-boot:run
   ```
2. Mở Swagger UI để test trực tiếp các API:
   👉 **`http://localhost:8080/api/v1/swagger-ui/index.html`**
